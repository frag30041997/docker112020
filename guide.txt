
installer git
	déjà présent sur ubuntu

mise en place git
	#marche pas localement
	
	mise en place git via repo créer github
	- git clone <url>
	#clonage marche pas
	
		#investigation.png
		créer lien manuel ssh
			#créer clé ssh
			- ssh-keygen -t rsa -b 4096 -C "frag30041997@gmail.com"
			
			#activer
			- eval "$(ssh-agent -s)"
			
			#ajouter clé
			- ssh-add ~/.ssh/id_rsa
			
			#verifier présence (doit avoir id_rsa et id_rsa.pub)
			- ls -lha /home/frag/.ssh/ total 20k
			
			#copier tout le contenu de id_rsa.pub
			- gedit ~/.ssh/id_rsa.pub
			
			aller sur github setting/SSH and GPG key
			ajouter le contenu copié dans la case script
			penser a mettre un titre evocateur
			
			- git clone git@github.com:frag30041997/docker112020.git
			sa marche enfin bordel
			#joie.jpg
			
			on a recupéré un fichier correspondant au repo
			j'ai mis un Dockerfile pour mettre un fichier et tester le push
			
			- git add .
			- git status
			- git commit -m "first12"
			
			- git push origin master
			#marche pas
			
			- git push
			#marche
			
installation docker
marche pas
et c'est repartis !
			
			
			
